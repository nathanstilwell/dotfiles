source ~/.castlebase/.path;
source ~/.castlebase/.colors;
source ~/.castlebase/.aliases;
source ~/.castlebase/.functions;

#
# Load System Specific Aliases
#
if [ -f ~/.profile ]; then
  . ~/.profile
fi
